---
const links = [
  { href: '/personal-blog/blog/', label: 'Blog' },
  { href: '/personal-blog/tags/', label: 'Topics' },
  { href: '/personal-blog/about/', label: 'About' }
];
---

<div class="flex flex-row gap-4">
  {
    links.map((link) => (
      <a href={link.href} class="hover:text-smalt no-underline">
        {link.label}
      </a>
    ))
  }
</div>
